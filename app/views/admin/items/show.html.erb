<h1>商品詳細</h1>
  <%= attachment_image_tag @item, :image, size: "300x200"  %>
  <table>
    <tr>
      <td>商品名</td>
      <td><%= @item.name %></td>
    </tr>
    <tr>
      <td>商品説明</td>
      <td><%= @item.introduction %></td>
    </tr>
    <tr>
      <td>ジャンル</td>
      <td><%= @item.genre.name %></td>
    </tr>
    <tr>
      <td>税込み価格<p>（税抜き価格）</p></td>
      <td><%= tax %>(<%= @item.price %>)円</td>
    </tr>
    <td><%= link_to "編集する", edit_admin_item_path(@item.id) %></td>
  </table>